export interface Playlist {
  id: string;
  name: string;
  public: boolean;
  description: string;
}


// Semigroup // Monoid 
'' + 'text' + 'text' + '';
0 + 123 + 123 + 123 + 0 ;
1 * 100 * 23* 23 * 23 * 1 ;
([] as number[]).concat([123]).concat([2323]).concat([]);

// Null Object Pattern

// [setState] = useState(EMPTY_PLAYLIST)
// setState(playlist)
