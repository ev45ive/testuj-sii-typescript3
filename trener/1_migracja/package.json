{
  "name": "1_migracja",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc ./src/*.ts --strict --outDir ./dist --lib \"esnext,dom\" --target esnext",
    "dev": "tsc --watch ./src/*.ts --strict --outDir ./dist --lib \"esnext,dom\" --target esnext"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
